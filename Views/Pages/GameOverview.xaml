<Page x:Class="Dartsmanager.Views.Pages.GameOverview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dartsmanager.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GameOverview">

    <Page.Resources>
        <DataTemplate x:Key="Template_Wedstrijden">
            <Grid Margin="0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <TextBlock Style="{StaticResource TextBlock_LB}" Text="{Binding Tournament.Naam}" Grid.Column="0"/>
                <TextBlock Style="{StaticResource TextBlock_LB}" Text="{Binding Tournament.Jaargang}" Grid.Column="1"/>
                <TextBlock Style="{StaticResource TextBlock_LB}" Text="{Binding Ronde}" Grid.Column="2"/>
                <TextBlock Style="{StaticResource TextBlock_LB}" Text="{Binding Player1.VoornaamNaam}" Grid.Column="3"/>
                <TextBlock Style="{StaticResource TextBlock_LB}" Text="{Binding Player2.VoornaamNaam}" Grid.Column="4"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TITEL -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Wedstrijden" Style="{StaticResource TextBlock_H2}" />

        <!-- ZOEKBOX -->
        <Border Grid.Row="1" Grid.Column="0" CornerRadius="10" BorderBrush="Black" BorderThickness="1" Background="#2B2B2B" Margin="5" MinWidth="200" >
            <TextBox x:Name="TB_Wedstrijd" Style="{StaticResource TextBox_Search}" Text="Zoek wedstrijd..." GotFocus="TB_Wedstrijd_GotFocus" LostFocus="TB_Wedstrijd_LostFocus" TextChanged="TB_Wedstrijd_TextChanged"/>
        </Border>
        <Button Grid.Row="1" Grid.Column="1" x:Name="BT_View_Game" Content="Bekijk wedstrijd" Click="BT_View_Game_Click" Style="{StaticResource Button_B3}" Foreground="Orange"/>

        <!-- HEADER -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4"  BorderBrush="Black" BorderThickness="0,0,0,1" Background="#2B2B2B" Margin="5,5,5,0" HorizontalAlignment="Left">
            <Grid  Margin="5,5,7,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Tornooi" Style="{StaticResource TextBlock_LB_Titel}" Grid.Column="0"/>
                <TextBlock Text="Jaargang" Style="{StaticResource TextBlock_LB_Titel}" Grid.Column="1"/>
                <TextBlock Text="Ronde" Style="{StaticResource TextBlock_LB_Titel}" Grid.Column="2"/>
                <TextBlock Text="Speler 1" Style="{StaticResource TextBlock_LB_Titel}" Grid.Column="3"/>
                <TextBlock Text="Speler 2" Style="{StaticResource TextBlock_LB_Titel}" Grid.Column="4"/>
            </Grid>
        </Border>


        <!-- LISTBOX -->
        <ListBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" x:Name="LB_Wedstrijden"  
                 BorderBrush="DarkGray" Style="{StaticResource ListBox_L1}" ItemTemplate="{StaticResource Template_Wedstrijden}" d:ItemsSource="{d:SampleData ItemCount=2}"/>


    </Grid>
</Page>
